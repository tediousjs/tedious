E.g. config for /.tedious/test-connection.json to run integration tests:

  {
    "config": {
        "server": "localhost",
        "authentication": {
            "type": "default",
            "options": {
                "userName": "test",
                "password": "test"
            }
        },
        "options": {
            "port": "localhost",
            "database": "master"
        }
    }
}

