const assert = require('chai').assert;
const { getParameterEncryptionMetadata } = require("../../../src/always-encrypted/get-parameter-encryption-metadata");

describe("get parameter encryption metadata", () => {
  
});
